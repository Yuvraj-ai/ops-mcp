{
  "name": "ops-mcp",
  "version": "0.1.0",
  "description": "AI-native ops assistant MCP server for investigating and resolving order/payment/inventory mismatches",
  "scripts": {
    "typecheck": "for f in src/db/*.ts src/tools/*.ts; do npx tsc --noEmit --skipLibCheck --target ES2022 --module CommonJS --moduleResolution node --esModuleInterop \"$f\" || exit 1; done",
    "build": "tsc",
    "postbuild": "node -e \"require('fs').copyFileSync('src/db/schema.sql','dist/db/schema.sql')\"",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "test": "tsx --test src/tests/*.test.ts",
    "seed": "tsx src/db/seed.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "@types/pg": "^8.11.0",
    "pg-mem": "^3.0.14",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4"
  }
}
